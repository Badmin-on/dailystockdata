name: Stock Data Auto Update

on:
  schedule:
    # 한국 시간 오전 7시 = UTC 22시 (전날)
    - cron: '0 22 * * *'
    # 한국 시간 오후 7시 = UTC 10시
    - cron: '0 10 * * *'

  # 수동 실행 버튼 추가
  workflow_dispatch:
    inputs:
      scraper_type:
        description: '실행할 스크립트 선택'
        required: true
        type: choice
        options:
          - fnguide
          - stock-price
          - both

jobs:
  # Job 1: FnGuide 재무 데이터 수집 (오전 7시)
  fnguide-scraper:
    name: FnGuide Financial Data Collection
    runs-on: ubuntu-latest
    # 조건: 오전 7시 스케줄(UTC 22:00) 또는 수동 실행(fnguide/both)
    if: |
      github.event.schedule == '0 22 * * *' ||
      github.event.inputs.scraper_type == 'fnguide' ||
      github.event.inputs.scraper_type == 'both'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: scripts/package.json

      - name: Install dependencies
        working-directory: ./scripts
        run: npm ci

      - name: Run FnGuide scraper
        working-directory: ./scripts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
        run: |
          echo "🚀 FnGuide 재무 데이터 수집 시작..."
          node fnguide-scraper.js
          echo "✅ FnGuide 재무 데이터 수집 완료!"

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fnguide-scraper-logs-${{ github.run_number }}
          path: scripts/*.log
          retention-days: 7

  # Job 2: 네이버 주가 데이터 수집 (오후 7시)
  stock-price-scraper:
    name: Naver Stock Price Collection
    runs-on: ubuntu-latest
    # 조건: 오후 7시 스케줄(UTC 10:00) 또는 수동 실행(stock-price/both)
    if: |
      github.event.schedule == '0 10 * * *' ||
      github.event.inputs.scraper_type == 'stock-price' ||
      github.event.inputs.scraper_type == 'both'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: scripts/package.json

      - name: Install dependencies
        working-directory: ./scripts
        run: npm ci

      - name: Run stock price scraper
        working-directory: ./scripts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
        run: |
          echo "🚀 네이버 주가 데이터 수집 시작..."
          node stock-price-scraper.js
          echo "✅ 네이버 주가 데이터 수집 완료!"

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: stock-price-scraper-logs-${{ github.run_number }}
          path: scripts/*.log
          retention-days: 7

  # Job 3: 실패 알림 (선택사항)
  notify-failure:
    name: Notify on Failure
    runs-on: ubuntu-latest
    needs: [fnguide-scraper, stock-price-scraper]
    if: failure()

    steps:
      - name: Send failure notification
        run: |
          echo "❌ 주가 데이터 수집 실패!"
          echo "수동 실행이 필요합니다."
          echo "실패한 Job: ${{ needs.fnguide-scraper.result }} / ${{ needs.stock-price-scraper.result }}"
          echo ""
          echo "📋 다음 단계:"
          echo "1. Actions 탭에서 로그 확인"
          echo "2. 로컬에서 수동 실행:"
          echo "   - node C:\\alexDB\\1_seoul_ys_fnguide_supabase.js"
          echo "   - node C:\\alexDB\\3_ys_stock_supabase.js"
